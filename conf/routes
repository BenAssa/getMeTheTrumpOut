# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
#GET     /                           controllers.EmployeeController.index()
GET     /vin/*path       controllers.EmployeeController.getVin(path: String)
GET     /url/*path        controllers.EmployeeController.getKeyWordsFromURL(path: String)
GET     /text/*path       controllers.EmployeeController.getKeyWordsFromText(path: String)

# Routes API Rest

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
